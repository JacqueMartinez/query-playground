export const headerBarStyles = {
  wrapper: 'flex items-center justify-between',
  logo: 'h-12 w-30',
  themeButton:
    'inline-flex h-9 w-9 items-center justify-center rounded-full border border-[var(--border-1)] bg-[var(--surface-2)] text-[var(--text-secondary)] transition hover:text-[var(--text-primary)]',
} as const;
